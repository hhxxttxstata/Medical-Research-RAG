[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: tests that require model loading or network
    integration: tests that exercise multiple modules
addopts = -v --tb=short -m "not integration"
