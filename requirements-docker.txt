# RAG 系统 Docker 部署依赖
# ⚠️  运行时依赖，保持与 app.py / src/* 导入一致

# 向量数据库
chromadb

# Embedding 模型
sentence-transformers>=5.4.0

# PDF 解析（可选：Marker 主路径，PyMuPDF 回退）
marker-pdf>=0.3.0

# LLM 生成
openai>=2.0.0

# 其他核心工具
python-dotenv>=1.2.0
rank-bm25>=0.2.2
tiktoken>=0.13.0
whoosh>=0.7.8

# Web 服务（API 部署）
fastapi>=0.135.0
uvicorn[standard]>=0.49.0
python-multipart>=0.0.20

# 安全防护（速率限制）
slowapi>=0.1.10

# 可观测性（Metrics + Tracing + 结构化日志）
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
structlog>=24.2.0

# 文件监听 + 缓存
watchdog>=6.0.0
redis>=5.0.0

# 医学影像推理（诊断模块）
nibabel>=5.0.0
scipy>=1.7.0
matplotlib
