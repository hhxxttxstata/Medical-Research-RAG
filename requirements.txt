# RAG 系统依赖
sentence-transformers==3.0.1
pypdf==4.3.1
# 不锁死小版本：==1.30.0 会与 ragas→instructor 的 openai/jiter 约束组合爆炸，pip 解析超 1 小时超时（CI 卡死根因）
openai>=1.30.0,<3
python-dotenv==1.0.1
rank-bm25>=0.2.2
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
torch>=1.10.0
watchdog>=6.0.0
redis>=5.0.0
whoosh>=0.7.8
pymilvus>=2.4.0
# LangGraph Runtime Adapter（Step 16：Agentic RAG orchestration runtime）
langgraph>=1.0.0
# RAG 评测（Ragas 交叉验证：eval/run_ragas.py）
ragas>=0.2.0
datasets>=2.14.0
# 可观测性（可选）
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-instrumentation-fastapi>=0.50b0
